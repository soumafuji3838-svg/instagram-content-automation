# v0.9.13

- 写真は人物のいない中立的な素材を検索し、画像審査を最大3候補に実施。不合格・審査失敗時は写真を採用しません。素材写真の注記を表示します。アップロード写真は既存の手動確認が必要です。
- 比較表の空欄・非開示・確認できずは承認不可。初回生成で最大1回の追加調査、自動改善でも情報不足を再調査対象にします。
- 公開Web情報をドメイン制限なく検索する指示へ変更。全サイトの網羅は保証しません。推測は根拠の出典と「推測」の表示を生成指示で必須化し、数字の捏造は認めません。推測表示の意味的検証はモデルに依存します。
- 企業の比較軸を主要事業・収益構造・直近の変化・就活での確認点に変更。
- 企業名をロゴに併記。ロゴ取得失敗時も企業名を表示します。ロゴ取得失敗の承認ブロックは維持します。
- グラフ数値をバーから分離、長いラベルを折り返し、要約本文の配置を調整。

## 適用

v0.9.12の変更も含みます。ZIPをプロジェクト直下に展開し、npm test を実行してください。
Previewで再生成してください。既存の画像や投稿済みInstagram画像は自動更新されません。
旧投稿の比較軸が変わるため、原稿編集保存より「調査から再生成」を使ってください。
企業レポートを生成する場合は投稿タイプで企業レポートを選択してください。

59テスト合格。グラフPNGの配置確認済み。実APIを使う写真審査と生成は未検証。デプロイ・実投稿は未実施です。
