# 1. Root Cause

- 写真は候補のキーワード一致に依存し、再取得・編集経路に共通の審査がなかった。
- モデルへの指示と出力検証が分離され、情報不足や企業対応の不一致を生成中に検出しきれていなかった。
- ロゴの既存記録からの復元がなく、空のロゴ集合が承認判定を通り得た。
- データ件数は3固定、描画は固定座標と文字列切り捨てだった。

# 2. Changed Files

src/layout.js, src/renderer.js, src/photo.js, src/logo.js, src/generator.js, src/quality.js, src/quality-repair.js, src/image-review.js, src/app.js, scripts/demo.js, public/index.html, package.json, package-lock.json, README.md, test/chart-layout.test.js, test/editorial-safety.test.js, test/pipeline-matrix.test.js, test/quality-repair.test.js, test/smoke.test.js

# 3. Implementation Details

- 写真はテーマ表現→オフィス→自然な勤務風景→建築の順に最大4検索・8候補を画像審査。感情・不自然さ・関連性を確認。描画前にも実際の画像を審査し、不合格時は差し替え検索する。
- 情報検索は公式、IR、採用、公的統計、報道、業界媒体、求人、その他複数情報へ広げる。推定は根拠と算出方法、読者向けの推定表記を要求する。
- 情報不足は最大1回の再調査と補完処理。合理的根拠がない場合は生成内容を捏造せず承認を止める。
- logos.jsonのsourceUrlと画像キャッシュを復元。取得失敗時はフォールバック全件を最大2巡。全企業のバッファ存在を描画前に検証する。
- コンテナ寸法から3〜5件を配置。3件はカード、4〜5件は縦積みの数値カード。比較表は企業数から列幅を計算。文字を収める処理を共通化し、最小文字サイズでも入らない場合はエラー。
- 企業比較ではグラフと比較表の件数・名前・ドメイン・順番を検証。
- 本番の5枚を一括画像審査し、検証成功を投稿へ保存。検証未完了の本番投稿は承認・投稿に進めない。
- SVG出力のため完全なCSS flex/gridではなく、コンテナ基準の計算レイアウト。従来の本文ブロックも切り捨てを廃止。

# 4. Validation / Tests

63テスト合格。5投稿タイプに加え企業比較3/4/5社、計7パターン35枚の1080×1350 PNGを検証用データで生成。長い企業名、桁数、欠損ロゴ、文字あふれ、企業対応の不一致、画像不合格の停止上限、既存logos.json優先復元を検証。

OpenAI/Pexelsキーが環境にないため、実在企業のWeb調査・写真取得から始まる本番通し生成は未実施。写真・ロゴAPIはモックを使用。検証PNGのロゴはテスト用図形、写真はプレースホルダー。実際の写真品質・事実確認を合格した成果物ではない。

# 5. Before / After

3件固定→3〜5件。長文切り捨て→折り返し・縮小または停止。ロゴなしでも描画→不足検知と再取得後に停止。写真の単純選択→候補審査・差し替え・描画後審査。画像検証記録なし→本番では検証必須。

# 6. Remaining Risks

- AI画像審査・根拠の意味的検証は誤判定があり、絶対的な再発ゼロは保証できない。
- Web全サイトの網羅は保証できない。API制限、実行時間制限で検索が完了しない場合は処理失敗になる。
- 公式ドメインの画像でもfaviconがロゴに不適切な場合があり、描画後審査に依存する部分がある。
- 5社の比較表はスマートフォンで細かい表示になる。長文は停止対象。
- 数値推定の算出方法はモデル指示と出典文字列チェックであり、独立した算術検算は未実装。
- ロゴ・描画検証失敗はエラーで停止する。あらゆる失敗を自動で解消するわけではない。

# 適用

ZIPには以前のv0.9.12〜14の変更も含む。作業ブランチへ展開してnpm test。Previewで実APIを設定し5タイプを新規生成して確認すること。既存投稿は再生成が必要。commit/push/デプロイ/Instagram実投稿は未実施。
