# v0.9.11 総合評価の自動改善

v0.9.10適用済みのブランチへZIPの中身を上書きし、npm test、サーバー再起動を実行してください。

## 使い方

不合格の既存投稿で「承認」を押すと、総合評価の再評価と自動改善が始まります。
原稿の修正・再調査は合計最大3回。再調査は最大1回です。
参照・最新性が不足していればWeb再調査を優先し、検索証拠から参照情報を作成し直します。
その他の不合格項目は既存の参照情報を使って文章を改善します。
各候補の文字数を自動調整し、実際に品質評価を再実行します。
判定は公開基準合格、不合格数、構造違反数、総合点の順で比較し、より良い候補を保持します。
点数の直接上書きや75点基準の引き下げは行いません。

改善結果・試行履歴・未解決理由は投稿のqualityRepairに保存されます。
完了通知に合格状況と点数が表示されます。
原稿・参照元・比較内容が変わり得るため、処理後は確認待ちへ戻ります。
内容と画像を確認し、画像チェックを記録して再度承認してください。

## 制限と検証範囲

- 本バージョンで追加した総合評価改善は承認時に実行します。生成時の従来の修正、編集保存時の文字数調整は維持します。
- 検索で十分な情報が得られない場合など、必ず合格する保証はありません。不合格は保留します。
- API呼び出しと追加費用が発生します。各改善には文字数調整・評価も含まれ、数分かかる場合があります。
- API失敗時は保持した候補とエラーを記録し、自動送信は行いません。
- ユーザーが選択した表紙は維持しますが、再調査後の内容との適合性を再確認してください。
- ローカルテスト52件合格。外部モデルを使った実原稿の改善、本番Vercelの実行時間内での完了は未検証です。
- 自動投稿の実送信・スケジューラーは未実装です。
