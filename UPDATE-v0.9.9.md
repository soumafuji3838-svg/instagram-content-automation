# v0.9.9 — グラフと画像の確認

## 適用

現在のmainから新しいブランチを作成し、ZIP内の同名ファイルだけを上書きしてください。
`.env`、投稿データ、認証情報は含まれていません。npm testで確認後、Previewへデプロイして検証してください。

## 変更

- 棒グラフの右端を780に固定し、数値を820〜1000の独立領域に配置。
- 表紙写真の検索語指定・再取得（直前の写真を除外）。候補一覧の選択UIは未実装。
- JPEG/PNG/WebPを650KBまでアップロード可能。サーバー側でデコードしJPEG化。
- 画像のテーマ適合性、誇張・嘲笑、業界への敬意、実写誤認、利用権の5項目を人が確認。
- 写真・内容・生成画像に紐づくチェック記録。編集・差し替え後は再確認が必要。
- 未確認画像は承認・投稿APIでもブロック。デモの実投稿もブロック。
- 公開済みの投稿の編集・再生成・画像差し替えをサーバー側でも禁止。

画像のAI審査は未実装です。5項目のチェックは手動確認であり、AIによる安全保証ではありません。
過去にInstagramへ公開した画像は更新されません。差し替え版は別の新規投稿として作成してください。
アップロード画像は復元のため投稿メタデータに保存します。大量運用時は専用ストレージへの分離が必要です。

## 検証手順

1. Preview（ドライラン）で新しい投稿を生成。
2. 2枚目の数値が棒と重ならないことを確認。
3. 表紙写真・誠実さチェックを開き、検索語で別写真を取得。
4. アップロードでも差し替え、画像が変更されることを確認。
5. 未チェックのまま承認し、拒否されることを確認。
6. 全5項目を確認・記録し、品質基準合格後に承認・ドライラン。
7. 編集後、画像の再確認が必要になることを確認。

## 自動投稿：準備段階

GET /api/auto-publish/plan は認証付きの読み取り専用候補判定です。
品質・画像確認・承認・過去の投稿履歴を検査します。enabled=false、willPublish=falseで、実投稿を行いません。
スケジューラー、無人の画像審査、原稿生成からの完全自動投稿は未実装です。
自動送信を有効化する前に、投稿ジョブの永続化、複数インスタンス間の排他制御、送信結果不明時の照合、停止機構が必要です。
現行の共有JSON保存は同時更新に対応していないため、自動送信は未接続です。手動投稿も連打・複数タブ同時操作は避けてください。
